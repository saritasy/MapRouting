# MapRouting
A hub-based delivery management system using Vite + React (TypeScript) and MySQL. Hub operators select areas on a map and assign them to delivery boys, who can serve only within those zones. This ensures efficient delivery operations with clear service boundaries.
